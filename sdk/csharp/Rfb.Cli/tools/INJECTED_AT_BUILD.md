# Injected at build time

The `rfb-cli` linux-x64 binary is copied here by the release pipeline
(`.github/workflows/release.yml`, nuget job) from the run's
`rfb-cli-linux-x64` artifact. It is not stored in git.
