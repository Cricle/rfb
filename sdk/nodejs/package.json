{
  "name": "rfb-sdk",
  "version": "0.0.1",
  "description": "RFB Node.js/TypeScript SDK: unified RfbClient facade over the forkd controller (HTTP/JSON) and the forkd guest (TCP NDJSON + ZBRT v1 binary frames) — mirror port of the Rust reference implementation",
  "type": "module",
  "main": "dist/client.js",
  "types": "dist/client.d.ts",
  "exports": {
    ".": {
      "types": "./dist/client.d.ts",
      "default": "./dist/client.js"
    },
    "./errors": {
      "types": "./dist/errors.d.ts",
      "default": "./dist/errors.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist/",
    "!dist/test",
    "README.md",
    "LICENSE-MIT",
    "LICENSE-APACHE"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && tsc -p tsconfig.json",
    "build:test": "tsc -p tsconfig.tests.json",
    "test": "npm run clean && npm run build:test && node dist/test/validation.test.js && node dist/test/zbrt.test.js && node dist/test/zbrt-connection.test.js && node dist/test/client.test.js && node dist/test/ndjson-stream.test.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "rfb",
    "sandbox",
    "forkd",
    "firecracker"
  ],
  "author": "Cricle <qnydhuaji@gmail.com>",
  "license": "MIT OR Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Cricle/rfb",
    "directory": "sdk/nodejs"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "^7.0.2"
  },
  "sideEffects": false
}
