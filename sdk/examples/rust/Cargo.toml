[package]
name = "rfb-quickstart"
version = "0.0.1"
edition = "2021"
publish = false

[dependencies]
# Published crate from crates.io — no path/workspace references.
rfb-sdk = "0.0.1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }

[workspace]
